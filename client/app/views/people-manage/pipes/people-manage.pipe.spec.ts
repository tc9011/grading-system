import { PeopleManagePipe } from './people-manage.pipe';

describe('PeopleManagePipe', () => {
  it('create an instance', () => {
    const pipe = new PeopleManagePipe();
    expect(pipe).toBeTruthy();
  });
});
